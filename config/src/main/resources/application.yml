spring:
  profiles:
     active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

server:
  port: 8888

security:
  user:
    name: user
    password: foo

